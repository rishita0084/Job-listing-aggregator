Search for jobs matching: $ARGUMENTS

Follow the job boards, workflow, and output format defined in CLAUDE.md.
Parse $ARGUMENTS to extract keyword and location if both are given (e.g. "python developer Bangalore" → keyword=python developer, location=Bangalore). If no location given, use location=India.